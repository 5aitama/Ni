/**
 * Dictionary where the keys type are string.
 */
 export type SDictionaryOf<T> = { [key: string] : T };

 /**
  * Dictionary where the keys type are number.
  */
 export type NDictionaryOf<T> = { [key: number] : T };